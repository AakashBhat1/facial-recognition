 # Mock Utilities

This directory contains mock/integration helper scripts used before the full face module is integrated.

## Current Scripts
- `facereq/mock_face_request.py`: sends contract-compliant requests to `POST /api/auth/recognize` for deny/grant scenario validation.
